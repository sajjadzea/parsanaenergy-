{
  "name": "parsanaenergy",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "docs",
    "decision-tree-app"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm --filter ./docs... run build"
  }
}
