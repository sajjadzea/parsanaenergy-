{
  "name": "parsanaenergy-root",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright install --with-deps && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
